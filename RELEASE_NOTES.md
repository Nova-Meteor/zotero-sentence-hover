# 句译随行 v1.0.1

修复“保存并测试翻译”可能显示 can't access dead object，而阅读器仍可翻译的问题。

## 修复内容

- 保存设置、调整外观和切换原文高亮时，跳过并清理已关闭或重载的 PDF 窗口。
- 阅读器关闭时立即使待处理的界面更新失效，避免请求返回后继续访问旧窗口。
- 清理过程中即使某个窗口对象失效，也会继续移除其他事件监听。
- 连接测试不再等待其他文章的缓存写入；附加诊断错误不会把已成功的翻译标记为失败。
- 设置窗口关闭后，测试请求完成时不再更新已销毁的控件。

## 更新方法

已安装 v1.0.0 的用户可在 Zotero 插件管理器中检查更新。也可下载本页的 **sentence-hover-1.0.1.xpi**，通过“工具 → 插件 → 齿轮 → Install Plugin From File…”覆盖安装。

安装后建议重启 Zotero。原有 API 配置和文章缓存保留。0.3.x 开发版需要手动安装一次，因为其更新地址尚未指向 GitHub。

## 验证

- 33 项设置页与阅读器测试通过，包括失效窗口、请求迟到、设置页关闭以及翻译成功后的诊断异常。
- 安装清单检查、XPI 结构检查通过。
- 测试基于模拟阅读器，实际 Zotero 环境仍需验证。
- 提供 SHA256SUMS.txt 校验文件；updates.json 用于 Zotero 更新检查。
