# 句译随行 v1.0.0

在 Zotero 内置 PDF 阅读器中，悬停英文句子即可查看中文翻译，并随鼠标所在单词联动高亮中文对应词语。

## 功能

- 悬停识别并翻译整句，弹窗居中显示、自动调整宽度。
- 中英文对应词语联动高亮；原文单词高亮可单独关闭。
- 字号、最大宽度和背景透明度可通过滑块调整并预览。
- 缓存保存在 PDF 所在目录，重启后可复用。
- 点击 ↻ 或按 Ctrl+Alt+R 重新翻译；macOS 使用 ⌘+Option+R。
- 自行配置 Chat Completions 兼容 API。

## 安装

1. 下载本 Release 附件中的 **sentence-hover-1.0.0.xpi**。
2. Zotero → 工具 → 插件 → 齿轮 → Install Plugin From File…，选择 XPI。
3. 编辑 → 设置 → 句译随行，配置 API 地址、模型名称和密钥。

升级安装保留原有配置和文章缓存。GitHub 自动生成的 Source code ZIP/TAR 是源码，不是插件安装包；请下载 XPI。

## 兼容性与验证

- 面向 Zotero 10.x 标准 PDF 视图，需要可提取文字的 PDF；扫描件需先 OCR。
- 跨页句子和复杂排版可能影响断句，翻译及词语对应质量取决于模型。
- 53 项自动化测试通过，覆盖设置交互、悬停行为、缓存读写及异常处理；不代表所有平台和服务商均已实测。
- 提供 SHA256SUMS.txt 校验文件。
